const card = {
  marginBottom: 2,
  width: '55%',
  transition: 'transform 0.3s ease',
  position: 'relative',
  '&:hover': {
    transform: 'scale(1.05)',
  },
};
  
const cardDetail = {
  marginBottom: 2,
  width: '100%',
};

const link = {
  textDecoration: 'none',
  color: 'inherit',
  display: 'block',
  width: '100%',
  height: '100%',
};

const cardContent = {
  background: 'black',
};

const styles = {
  card,
  cardDetail,
  link,
  cardContent,
};

export default styles;
  