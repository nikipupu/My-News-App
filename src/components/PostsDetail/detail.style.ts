const detailContainer = {
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
};

const cardDetail = {
  width: '80%',
  marginBottom: 4,
};

const styles = {
  detailContainer,
  cardDetail,
};

export default styles;
